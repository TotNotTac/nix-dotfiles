{ config, pkgs, lib, ... }:
{
  nixpkgs.overlays = [
    (self: super: { discord-canary = super.discord.overrideAttrs (_: { src = builtins.fetchTarball "https://discordapp.com/api/download/canary?platform=linux&format=tar.gz";});})
  ];
}
